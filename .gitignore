node_modules/
dist/
# Bulk ticker series live in Cloud Storage, not git
data/tickers/
app/public/data/
.env
.env.*
# No secrets in it — just the public data bucket URL
!app/.env.production
*.local
.DS_Store
