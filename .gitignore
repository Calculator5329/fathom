node_modules/
dist/
.firebase/
# Derived/fetchable data lives in Cloud Storage, not git (re-runnable scripts)
data/tickers/
data/fundamentals/
app/public/data/
.env
.env.*
# No secrets in these — just public URLs
!app/.env.production
!app/.env.development
*.local
.DS_Store
